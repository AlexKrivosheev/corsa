@model Corsa.Models.Moduls.ProjectModuleViewModel
<div class="modul-selector-item col-lg-3 col-md-6 col-sm-12 mb-4 " onclick='location.href="@Url.Action(Model.ViewDescriptor.OpenAction,Model.ViewDescriptor.Controller,new { id = Model.Module.Id })"'>
    <div class="card text-white bg-primary">
        <div class="card-body">            
            <p class="project-module-identity"> <i class="fas fa-chart-line icon"></i> @Model.ViewDescriptor.Name </p>
            <h5 class="card-title">@Model.Module.Name</h5>
            <p class="card-text">@Model.Module.Description</p>

            <div class="bar-actions">
                <ul class="nav justify-content-end">
                    <li class="nav-item action ml-3">
                        <a href="@Url.Action("Edit","SerpAnalysis",new {Id=Model.Module.Id})"><i class="fas fa-pencil-alt"></i></a>
                    </li>
                    <li class="nav-item action ml-3">
                        <a href="@Url.Action("Delete","SerpAnalysis",new {Id=Model.Module.Id})"><i class="fas fa-trash"></i></a>
                    </li>

                </ul>
            </div>
        </div>
    </div>
</div>