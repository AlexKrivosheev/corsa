@model Corsa.Models.Moduls.ProjectModuleViewModel
<div class="modul-selector-item col-lg-3 col-md-6 col-sm-12 mb-4 " onclick='location.href="@Url.Action(Model.ViewDescriptor.OpenAction,Model.ViewDescriptor.Controller,new { id = Model.Module.Id })"'>
    <div class="crs-email-notif-showcase card">
        <div class="card-body">
            <p class="project-module-identity"> <i class="crs-email-notif-showcase__icon fas fa-envelope"></i> @Model.ViewDescriptor.Name </p>
            <h5 class="crs-email-notif-showcase__title card-title">@Model.Module.Name</h5>
            <p class="crs-email-notif-showcase__desc card-text">@Model.Module.Description</p>

            <div class="crs-email-notif-showcase__action bar-actions">
                <ul class="nav justify-content-end">
                    <li class="nav-item action ml-3">
                        <a href="@Url.Action("Edit","EmailNotif",new {Id=Model.Module.Id})"><i class="fas fa-pencil-alt"></i></a>
                    </li>
                    <li class="nav-item action ml-3">
                        <a href="@Url.Action("Delete","EmailNotif",new {Id=Model.Module.Id})"><i class="fas fa-trash"></i></a>
                    </li>

                </ul>
            </div>
        </div>
    </div>
</div>